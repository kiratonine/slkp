{
  "name": "s1lk-x402-bridge",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "start:api": "pnpm --filter api start:dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}